# Normalize all text to LF in the repo and on checkout, on every platform.
# Keeps managed files (shipped to downstream repos and read by AI agents)
# byte-stable, and silences "LF will be replaced by CRLF" for contributors on
# Windows. text=auto lets git skip detected-binary files; eol=lf forces LF.
* text=auto eol=lf
