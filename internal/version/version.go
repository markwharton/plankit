// Package version provides build version information.
// The Version variable is set at build time via ldflags.
package version

// Version is the build version, set via -ldflags at compile time.
var Version = "dev"
