# Build output
dist/
/pk
# Stray binaries from `go build ./evals/<tool>` (use `go run`); the main-package name lands at root
/calibrate
/footprint

# Sketches
_sketch*

# Editor and OS
*.bak
.DS_Store
